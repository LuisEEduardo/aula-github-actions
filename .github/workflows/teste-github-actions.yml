name: Exemplo de workflow

on:
  workflow_dispatch:


jobs:
  acao_inicial_job:
    runs-on: ubuntu-latest
    steps:
      - name: Imprime Hello world
        run: echo Hello world
      - name: Imprime
        run: echo Tchau

